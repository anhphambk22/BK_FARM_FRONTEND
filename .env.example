# Frontend MQTT configuration (browser WebSocket)
# Dev: ws:// is fine when your site runs over http:// (e.g., Vite dev server)
# Prod: if your site is served over HTTPS, browsers will block ws:// (mixed content).
#       In production, expose the broker via WSS (TLS) or proxy it to WSS.
VITE_MQTT_URL=ws://broker.local:8083/mqtt
VITE_MQTT_USERNAME=your_user
VITE_MQTT_PASSWORD=your_pass

# Topic mapping: set to the exact topics your devices publish
# Payloads should be numeric (e.g., "25.6"); non-numeric are ignored
VITE_MQTT_T_AIR=devices/bkfarm/air/temperature
VITE_MQTT_T_AIR_H=devices/bkfarm/air/humidity
VITE_MQTT_T_LIGHT=devices/bkfarm/light
VITE_MQTT_T_SOIL_T=devices/bkfarm/soil/temperature
VITE_MQTT_T_SOIL_M=devices/bkfarm/soil/moisture
VITE_MQTT_T_SOIL_PH=devices/bkfarm/soil/ph
VITE_MQTT_T_N=devices/bkfarm/soil/nitrogen
VITE_MQTT_T_P=devices/bkfarm/soil/phosphorus
VITE_MQTT_T_K=devices/bkfarm/soil/potassium
